#include<bits/stdc++.h>
using namespace std; 
#define int long long 
#define el "\n" 

// fast fibonacci
// O(logn)
// MUST use matrix exponentiation because sqrt(5) 
// formula is too easily prone to rounding errors 
struct matrix {
    int mat[2][2];
    matrix friend operator *(const matrix &a, const matrix &b){
        matrix c;
        for (int i = 0; i < 2; i++) {
          for (int j = 0; j < 2; j++) {
              c.mat[i][j] = 0;
              for (int k = 0; k < 2; k++) {
                  c.mat[i][j] += a.mat[i][k] * b.mat[k][j];
              }
          }
        }
        return c;
    }
};

matrix matpow(matrix base, int n) {
    matrix ans{ {
      {1, 0},
      {0, 1}
    } };
    while (n) {
        if(n&1)
            ans = ans*base;
        base = base*base;
        n >>= 1;
    }
    return ans;
}

int fib(int n) {
    matrix base{ {
      {1, 1},
      {1, 0}
    } };
    return matpow(base, n).mat[0][1];
}

signed main(){
    int n; cin >> n; 
    cout << fib(n) << el;
    return 0; 
} 